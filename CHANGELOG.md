# 更新日志

## v2.5.0

本次版本聚焦更新流程、Electron 与 Python 引擎生命周期、文件访问安全和批量处理稳定性，并完成了一轮覆盖主进程、渲染端与 Python 引擎的全局代码审查和回归修复。

### 新增

- 新增安装版软件更新流程：在“设置”页面检查正式版本，发现更新后展示版本号和更新说明，用户可手动开始下载，查看下载进度、已传输大小和速度，下载完成后确认重启并安装。
- 新增更新状态机，覆盖检查中、发现新版本、下载中、下载完成、安装中、已是最新、暂不支持和错误等状态；更新检查与下载请求分别增加并发去重，避免重复发起网络请求。
- 新增便携版、压缩包版和开发环境的更新降级状态，避免不适合自动覆盖的版本误触安装流程。
- 新增文件访问 IPC 的结构化结果格式，统一返回成功值或带错误码的失败结果，便于区分未授权、路径不存在、不是文件、不是目录、文件过大、类型不支持和权限不足等情况。
- 新增文件预览自定义协议，参考图片通过受控的本地流式 URL 加载，不再将整个文件编码成 Base64 字符串后经 IPC 传输。
- 新增二维码和印章识别诊断信息，记录解码器异常、OpenCV 图像处理失败、颜色空间转换失败、形态学处理失败和轮廓分析失败等降级原因。
- 新增扫描拆分单页测试结果中的检测诊断、耗时统计和识别阶段信息，便于调参和定位漏检原因。
- 新增重命名页大文件列表的窗口化渲染，减少一次性渲染数千行文件记录带来的页面开销。
- 新增共享的插入编号规则，插入规则页和自定义规则页使用同一份编号配置，切换页面时不会静默覆盖初始值、步进值和位数。
- 新增共享面板横幅、文件名处理、数字输入、重命名规则查找和更新工具函数，统一三个主要功能面板的基础交互。
- 新增结构化 API 类型定义，覆盖 Electron preload、Python engine 调用、重命名规则、PDF 配置、扫描选项、文件访问结果和更新状态。
- 新增历史记录详情大小限制、列表截断标记和大字段压缩信息，日志页面可以明确显示记录是否被截断。
- 新增 28 项 Python 回归测试，覆盖 PDF 输出并发、任务取消、扫描日志、二维码识别、ROI 处理、OpenCV 状态恢复和历史记录写入等场景。

### 优化

- 优化 Python 引擎启动、重启和退出流程：等待旧 bridge 完成关闭后再启动新进程，并使用启动去重锁避免并发创建多个 Python 子进程。
- 优化引擎启动失败和重启失败状态，错误会同步到渲染端状态栏，不再让界面长期停留在“启动中”。
- 优化 PythonBridge 的 JSON-RPC 处理，区分响应、通知和 ready 消息，忽略非对象 JSON，清理已关闭 bridge 的通知处理器，并缩短任务取消请求的超时时间。
- 优化 Electron 路径授权模型，区分文件授权和目录授权；目录授权只覆盖其子路径，授权记录增加数量上限，窗口关闭时清理当前窗口的授权状态。
- 优化外部链接安全策略，仅允许 HTTPS 和 GitHub 官方域名，更新说明中的链接继续经过标签、属性和协议白名单清洗。
- 优化更新页面状态展示，版本号统一来自 Electron `app.getVersion()`，错误状态不再与旧版本更新说明混合显示；更新页保留检查、下载和安装操作，项目地址统一放在 About 页面。
- 优化重命名页文件数量上限处理，达到 5000 个文件后只保留实际允许加入的路径，不再把超限路径留在临时集合中。
- 优化重命名页文件大小查询，使用可变 Map 配合精确触发更新，减少大批量文件加入时的整体 Map 拷贝。
- 优化 PDF 拆分预览生命周期，正式执行拆分时清理过期预览，避免执行期间继续展示旧的输出计划。
- 优化扫描拆分任务通知监听，将引擎状态和任务事件纳入同一订阅和清理流程，减少任务完成、取消和引擎异常之间的状态竞态。
- 优化扫描拆分参数日志，记录检测模式、DPI、ROI、二维码识别强度、特征点参数、分段限制和加速选项，便于复现一次扫描任务的实际配置。
- 优化二维码和印章诊断日志，同一轮扫描中相同降级原因只记录一次，避免大量重复日志淹没真正有用的信息。
- 优化历史记录保存流程，使用原子临时文件写入、写入锁和失败退避；磁盘写入失败时不会每隔固定时间无限高频重试。
- 优化历史记录清空流程，先确认落盘成功再返回成功状态，持久化失败时恢复内存中的原有记录。
- 优化 preload 和渲染端的类型边界，移除主要 engine API 和文件 IPC 中的显式 `any`，统一未知错误的前端格式化处理。
- 优化面板无障碍语义，补充导航当前页、状态栏实时区域、下拉框 ARIA 属性、对话框焦点恢复和 ROI 对话框键盘焦点管理。
- 优化 Electron 主进程未捕获异常处理，记录致命异常后退出进程，避免主进程继续处于不可预测状态。
- 优化 Python 生产错误返回，完整 traceback 写入引擎日志；只有开发调试模式才向客户端返回调试信息，减少路径和环境信息泄露。
- 优化 Python 依赖版本约束，为核心依赖增加上限，降低未来依赖 breaking change 直接影响发布包的风险。

### 修复

- 修复普通 PDF 拆分结果中的 `output_files` 始终为空的问题，现在会汇总每个拆分操作实际生成的输出文件。
- 修复重命名文件数量达到上限时仍保留超限临时路径的问题，避免后续预览和执行阶段处理超过上限的文件。
- 修复插入编号规则在插入页和自定义页之间切换时配置被覆盖的问题。
- 修复更新页使用硬编码版本号的问题，About 页面、更新面板和状态信息现在保持同一版本来源。
- 修复便携版、压缩包版和开发环境点击“检查更新”后状态没有变化的问题；所有版本现在都会检查最新正式版本，非安装版发现更新时提供发布页入口。
- 修复更新页错误状态仍显示旧 release notes 的问题，检查或下载失败时改为独立显示错误和重试操作。
- 修复更新页同时出现多个发布页入口的问题，项目外链统一收敛到 About 页的 GitHub 地址。
- 修复更新页 release notes 缓存只使用正文长度导致不同内容可能命中同一缓存的问题，改为使用正文内容作为缓存键。
- 修复更新安装前 Python bridge 关闭失败时被静默忽略的问题，关闭失败会恢复 bridge、报告错误并中止安装。
- 修复 PDF 拆分正式执行后旧预览计划残留的问题，执行开始时清理旧预览内容。
- 修复任务状态双重订阅和清理不完整的问题，避免引擎异常后任务状态卡住或回调重复触发。
- 修复二维码检测向调用方提供诊断字典时无条件清空已有字段的问题，保留调用方预填充信息。
- 修复二维码解码失败、印章颜色转换失败和视觉处理降级路径缺少日志的问题，相关信息现在会进入扫描日志和历史记录。
- 修复 Python JSON-RPC 通知请求被错误地返回响应的问题，无 `id` 的通知不再生成响应。
- 修复 Python 异步扫描任务、单页测试任务和参考图预览错误向生产客户端返回完整 traceback 的问题。
- 修复历史记录后台写入失败后 `_dirty` 状态长期保持并反复快速失败的问题，增加指数退避并保留下一次重试机会。
- 修复历史记录详情包含大量输出文件、日志或操作记录时可能无限增长的问题，超限字段现在会被截断并标记原始数量。
- 修复文件 IPC 将“未授权”“不存在”和“访问失败”都返回为空结果的问题，渲染端现在可以针对错误类型给出不同提示。
- 修复参考图片预览整文件 Base64 经 IPC 传输造成额外内存峰值的问题，改为自定义协议流式读取。
- 修复 `PythonBridge` 收到非法 JSON 或非对象 JSON 时可能触发异常的问题，异常输入现在会被安全忽略并记录警告。

## v2.4.0

本次版本聚焦 PDF 依赖迁移、Python 引擎稳定性、任务取消保留输出、更新检查逻辑和界面细节修复，同时同步完善 Windows 打包配置。

### 新增

- 新增普通拆分取消后的输出文件汇总字段，前端可稳定提示已保留的生成文件数量。
- 新增更新检查对 GitHub 正式 Release 的筛选逻辑，自动忽略草稿版和预发布版。
- 新增 `CHANGELOG.md`，用于记录版本更新内容并复用到 GitHub Release 发布说明。

### 优化

- 将 PDF 处理依赖从 `PyPDF2` 迁移到 `pypdf`，普通拆分、扫描拆分、智能重命名和打包配置已统一同步。
- 普通 PDF 拆分和扫描拆分取消后会保留已生成文件，减少长任务中途取消时的重复处理成本。
- 优化 Electron 与 Python 引擎启动链路，开发环境会跳过损坏的虚拟环境并回退到可用 Python。
- 优化普通拆分预览空状态、设置页更新状态和更新日志展示样式，减少界面嵌套和视觉杂乱。
- 优化重命名页和普通拆分页右侧分段按钮切换动画，切换模式时更自然。
- 优化左侧导航布局，恢复固定宽度并移除无效折叠按钮。
- 优化 Python 依赖约束，恢复为仅设置最低版本，改善 Python 3.14 环境下的安装兼容性。
- 优化构建要求说明，明确 Node.js 版本需要 `20.19+` 或 `22.12+`。

### 修复

- 修复更新检查在当前版本尚未发布时仍显示旧版本日志的问题。
- 修复普通拆分 `pdf_paths` 校验遗漏，避免无效 PDF 路径进入后端执行阶段。
- 修复路径授权中空字符串可能导致校验范围异常的问题。
- 修复扫描拆分字符串布尔值解析问题，避免 `false` 被误判为启用。
- 修复普通拆分页码范围输入无效时可能静默回退为整本 PDF 输出的问题。
- 修复 PDF 输出页码越界时被静默钳制的问题，改为明确报错。
- 修复 Python 引擎任务取消、队列释放和完成通知中的状态残留风险。
- 修复 Python 引擎鉴权比较在异常字符输入下可能抛错的问题。
- 修复 PyInstaller 打包路径配置，确保 Python 引擎能正确加载项目内 `src.*` 模块。
- 修复重命名自定义规则输入框右侧空缺和宽度不一致的问题。
