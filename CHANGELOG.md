# 更新日志

## v2.4.0

本次版本聚焦 PDF 依赖迁移、Python 引擎稳定性、任务取消保留输出、更新检查逻辑和界面细节修复，同时同步完善 Windows 打包配置。

### 新增

- 新增普通拆分取消后的输出文件汇总字段，前端可稳定提示已保留的生成文件数量。
- 新增更新检查对 GitHub 正式 Release 的筛选逻辑，自动忽略草稿版和预发布版。
- 新增 `CHANGELOG.md`，用于记录版本更新内容并复用到 GitHub Release 发布说明。

### 优化

- 将 PDF 处理依赖从 `PyPDF2` 迁移到 `pypdf`，普通拆分、扫描拆分、智能重命名和打包配置已统一同步。
- 普通 PDF 拆分和扫描拆分取消后会保留已生成文件，减少长任务中途取消时的重复处理成本。
- 优化 Electron 与 Python 引擎启动链路，开发环境会跳过损坏的虚拟环境并回退到可用 Python。
- 优化普通拆分预览空状态、设置页更新状态和更新日志展示样式，减少界面嵌套和视觉杂乱。
- 优化重命名页和普通拆分页右侧分段按钮切换动画，切换模式时更自然。
- 优化左侧导航布局，恢复固定宽度并移除无效折叠按钮。
- 优化 Python 依赖约束，恢复为仅设置最低版本，改善 Python 3.14 环境下的安装兼容性。
- 优化构建要求说明，明确 Node.js 版本需要 `20.19+` 或 `22.12+`。

### 修复

- 修复更新检查在当前版本尚未发布时仍显示旧版本日志的问题。
- 修复普通拆分 `pdf_paths` 校验遗漏，避免无效 PDF 路径进入后端执行阶段。
- 修复路径授权中空字符串可能导致校验范围异常的问题。
- 修复扫描拆分字符串布尔值解析问题，避免 `false` 被误判为启用。
- 修复普通拆分页码范围输入无效时可能静默回退为整本 PDF 输出的问题。
- 修复 PDF 输出页码越界时被静默钳制的问题，改为明确报错。
- 修复 Python 引擎任务取消、队列释放和完成通知中的状态残留风险。
- 修复 Python 引擎鉴权比较在异常字符输入下可能抛错的问题。
- 修复 PyInstaller 打包路径配置，确保 Python 引擎能正确加载项目内 `src.*` 模块。
- 修复重命名自定义规则输入框右侧空缺和宽度不一致的问题。
